# Fuentes locales

Coloca aquí el archivo `Bigger.otf` (Bigger Display) para activar la
tipografía display preferida del portfolio.

Mientras no exista, el sitio usa automáticamente **Big Shoulders Display**
(Google Fonts) como fallback — está declarado así en `src/index.css`
(`@font-face` + `--font-display`).
